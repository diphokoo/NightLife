rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Event images: public read, authenticated write (admin check done client-side)
    match /events/{imageFile} {
      allow read: if true;
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
  }
}
